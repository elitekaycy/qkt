# qkt project secrets — copy to .env. The minimal template runs qkt with no
# broker — useful for backtest/paper exploration. Add broker entries later by
# extending qkt.config.yaml and editing docker-compose.yml.
QKT_IMAGE_TAG=v{{QKT_VERSION}}
