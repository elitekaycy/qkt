# Example strategies

These files are examples only. They are not mounted as live strategies.

To use one, copy it into `../../strategies/`, review/edit risk and symbols, then
run:

```bash
make preflight STRAT=<name>
make deploy STRAT=<name>
```
